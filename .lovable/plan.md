# Update Promo Banner Wording

## Change
Update the three marquee messages in `src/components/PromoBanner.tsx` (and the matching screen-reader line) to the punchier set:

1. `CALL NOW AND RESERVE` → `CALL TO RESERVE TODAY`
2. `STOCK ALWAYS CHANGING - CALL TO SECURE` → `STOCK MOVES FAST — CALL TO SECURE`
3. `HUGE SALE ACTIVE` → `HUGE SALE ON NOW`

## Files
- `src/components/PromoBanner.tsx` — `MESSAGES` array + `PromoBannerStatic` screen-reader text
